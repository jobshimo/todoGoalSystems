import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.todogoalsystems.app',
  appName: 'todoGoalSystems',
  webDir: 'dist/todo-goal-systems',
  bundledWebRuntime: false
};

export default config;
